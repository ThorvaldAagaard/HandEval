import { SelectedCardsPipe } from './selected-cards.pipe';

describe('SelectedCardsPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedCardsPipe();
    expect(pipe).toBeTruthy();
  });
});
